# 复制为 .env 后，Docker Compose 会自动读取这些值：
#   cp .env.example .env
#
# 默认值已经与 Java 配置完全对齐；第一次学习时建议不要修改端口。
# 如果修改 13306~13310，还必须同步修改 application-*.properties、
# shardingsphere-standard.yaml 和 shardingsphere-tenant.yaml。

# 固定镜像补丁版本，避免某一天重新拉取镜像后行为悄悄变化。
MYSQL_IMAGE=mysql:8.0.42

# 仅用于本机教学环境。真实项目必须交给 Secret Manager，不要把真实密码提交到 Git。
MYSQL_ROOT_PASSWORD=root-password

# 五个实例只绑定到 127.0.0.1，不向局域网暴露。
LEGACY_MYSQL_PORT=13306
DS0_PRIMARY_PORT=13307
DS0_REPLICA_PORT=13308
DS1_PRIMARY_PORT=13309
DS1_REPLICA_PORT=13310

# 固定 Compose 项目名，便于在 docker ps / docker volume ls 中识别本 Demo 资源。
COMPOSE_PROJECT_NAME=asset-sharding-demo
